import {
  $, hasClass, addClass, removeClass, attr,
  on, off, find, append, css, prop, setNode
} from './dom.js'

import { 
  wt, getType, each,
  add, minus,
  cookie,
  serialize,
  json 
} from './utils.js'












export {
  // dom
  $, hasClass, addClass, removeClass, attr,
  on, off, find, append, css, prop, setNode,

  // 工具
  wt, getType, each,
  add, minus,
  cookie,
  serialize,
  json
}