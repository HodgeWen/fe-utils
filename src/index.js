import {
  $, hasClass 
} from './dom.js'

import { 
  wt, getType, each,
  add, minus,
  cookie,
  serialize,
  json 
} from './utils.js'












export {
  // dom
  $, hasClass,

  // 工具
  wt, getType, each,
  add, minus,
  cookie,
  serialize,
  json
}