import {
  $, hasClass, addClass, removeClass, attr,
  on, off, find
} from './dom.js'

import { 
  wt, getType, each,
  add, minus,
  cookie,
  serialize,
  json 
} from './utils.js'












export {
  // dom
  $, hasClass, addClass, removeClass, attr,
  on, off, find,

  // 工具
  wt, getType, each,
  add, minus,
  cookie,
  serialize,
  json
}